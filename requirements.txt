# Core dependencies for both Admin Bot and User Bot
PyJWT>=2.8.0
requests>=2.31.0
python-dotenv>=1.0.0
Flask>=3.0.0
aiogram>=3.3.0
aiohttp>=3.9.0

# Admin Bot – PostgreSQL support (optional, but recommended)
asyncpg>=0.29.0

# Admin Bot – SQLite async support (fallback, if using SQLite)
aiosqlite>=0.19.0
